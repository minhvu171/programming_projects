The ECG\_signal\_processing\_workflow folder contains a script that loads, cleans, and analyzes ECG data from a CSV file (samples.csv). The Machine\_Learning\_development\_workflow folder contains a script that implements an end-to-end supervised learning workflow for binary classification on a tabular dataset (a diabetes outcomes dataset, diabetes.csv).

